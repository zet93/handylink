---
phase: 11-app-design
plan: 02
type: execute
wave: 2
depends_on: [11-app-design/11-01-PLAN.md]
files_modified:
  - frontend/src/pages/JobsPage.jsx
  - frontend/src/pages/JobDetailPage.jsx
  - frontend/src/components/JobCard.jsx
  - frontend/src/components/WorkerCard.jsx
autonomous: true
requirements: [DSG-03, DSG-05]

must_haves:
  truths:
    - "Web job/worker browse and bid flows are self-explanatory with clear trust signals."
    - "Layouts adapt at 640px, 1024px, 1280px viewports without overflow or misalignment."
    - "Worker cards show rating, review count, and job count as visible trust indicators."
  artifacts:
    - path: "frontend/src/components/WorkerCard.jsx"
      provides: "Trust signal UI"
      contains: "rating", "reviewCount", "jobCount"
    - path: "frontend/src/pages/JobsPage.jsx"
      provides: "Browse job grid / swipe stack layout"
      contains: "grid-cols" or "flex-wrap"
    - path: "frontend/src/pages/JobDetailPage.jsx"
      provides: "Flow guidance for bidding"
      contains: "Submit Bid" button text.
  key_links:
    - from: "2-01-PLAN" to "WorkerCard.jsx"
      via: "trust signal requirements "
      pattern: "rating|reviewCount|jobCount"
---

<objective>
Improve web UX to satisfy design flow clarity and responsive behavior requirements.

Purpose: Ensure web Customer can browse jobs/workers, see trust signals, and  in-flow interactions require no extra explanation.
Output: Updated job and worker cards, responsive page layouts, and page flow copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-app-design/11-CONTEXT.md
@.planning/phases/11-app-design/11-RESEARCH.md

<interfaces>
From frontend/src/components/WorkerCard.jsx approximate props:
  - worker
    - rating
    - reviewCount
    - jobCount
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trust signals to WorkerCard and JobCard</name>
  <files>frontend/src/components/WorkerCard.jsx, frontend/src/components/JobCard.jsx</files>
  <action>
    Extend WorkerCard props and rendering with badge row:
      - rating (e.g., 4.8 ⭐)
      - reviewCount (e.g., 18 reviews)
      - jobCount (e.g., 76 jobs)
    Add accessible labels and aria-labels.
    Update JobCard to include `featured` and`summary` text that sets expectation for next action.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --filter "WorkerCard" --runInBand</automated>
  </verify>
  <done>WorkerCard and JobCard show trust signals, and tests verify text present.</done>
</task>

<task type="auto">
  <name>Task 2: Make jobs/workers pages responsive and flow-friendly</name>
  <files>frontend/src/pages/JobsPage.jsx, frontend/src/pages/JobDetailPage.jsx</files>
  <action>
    JobsPage:
      - set grid using Tailwind: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`.
      - add spacing, consistent card widths, reduce margins on mobile.
    JobDetailPage:
      - ensure action button is sticky at bottom on small screens.
      - rewrite task instructions with direct next-step text: "Submit bid" button text, plain status labels.
    Add a non-technical copy block on JobDetail page: "Tap Bid to contact worker".
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --filter "JobsPage" --runInBand</automated>
  </verify>
  <done>Layouts are responsive and include clear user guidance copy.
</task>

</tasks>

<verification>
- `WorkerCard.jsx` renders rating/review/job count text.
- Jobs page grid adapts at breakpoints (checked by automated  snapshot test or UI check script).
- Job detail has explicit guidance copy and visible bid call-to-action.
</verification>

<success_criteria>
- DSG-03 and DSG-05 are satisfied for web flows.
- No breakage in existing frontend tests after layout changes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-app-design/11-02-SUMMARY.md`
</output>
