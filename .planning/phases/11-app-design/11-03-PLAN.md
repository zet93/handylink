---
phase: 11-app-design
plan: 03
type: execute
wave: 2
depends_on: [11-app-design/11-01-PLAN.md]
files_modified:
  - mobile/app/(client)/browse-workers.tsx
  - mobile/app/(client)/job-detail.tsx
  - mobile/app/(client)/post-job.tsx
autonomous: true
requirements: [DSG-03, DSG-04]

must_haves:
  truths:
    - "Mobile screens show no text/image overflow on popular phone sizes (360x640, 390x844)."
    - "Job detail and post-job flows are clear and do not require external instructions."
    - "Design tokens from `mobile/app/constants/design.ts` are used in all components."
  artifacts:
    - path: "mobile/app/(client)/browse-workers.tsx"
      provides: "Worker browse list with card layout"
      contains: "jobCount", "rating", "reviewCount"
    - path: "mobile/app/(client)/job-detail.tsx"
      provides: "Bid action and flow copy"
      contains: "Submit bid"
    - path: "mobile/app/(client)/post-job.tsx"
      provides: "Post job form layout"
      contains: "no overflow"
  key_links:
    - from: "mobile/app/(client)/browse-workers.tsx"
      to: "mobile/app/constants/design.ts"
      via: "token import + style object"
      pattern: "palette\."
---

<objective>
Polish the mobile app’s browse and job flows to meet design requirements and eliminate layout breakage.

Purpose: close DSG-03 and DSG-04 on mobile by explicit flow affordances and verified responsive layouts.
Output: updated screen components with token-based style usage, plus a Playwright mobile viewport test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-app-design/11-CONTEXT.md
@.planning/phases/11-app-design/11-RESEARCH.md

<interfaces>
From mobile/app/(client)/browse-workers.tsx and job-detail.tsx, use style objects with constants from `design.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply design tokens in mobile screens</name>
  <files>mobile/app/(client)/browse-workers.tsx, mobile/app/(client)/job-detail.tsx, mobile/app/(client)/post-job.tsx</files>
  <action>
    Import `palette` and `typography` from `mobile/app/constants/design.ts`.
    Update style objects in each screen to use tokens e.g. `backgroundColor: palette.background`, `color: palette.text`, `fontSize: typography.bodySize`.
    Add rating/review/job count to worker card items.
  </action>
  <verify>
    <automated>cd mobile && npx expo-yarn-workspaces test -- --filter "browse-workers|job-detail" || true</automated>
  </verify>
  <done>Mobile screens use design tokens for color and typography and include trust signal information.</done>
</task>

<task type="auto">
  <name>Task 2: Add layout guardrails and responsive checks for mobile views</name>
  <files>mobile/app/(client)/browse-workers.tsx, mobile/app/(client)/job-detail.tsx, e2e/playwright.config.ts</files>
  <action>
    Add style constraints: `flexWrap: 'wrap'`, `paddingHorizontal: 16`, `maxWidth: 100%` on cards.
    Add accessibility text labels for main actions: "Browse workers", "Submit bid", "Post job".
    Create or update `e2e/tests/mobile-layout.spec.ts` with viewport tests using `page.setViewportSize({ width: 390, height: 844 })` and assert no element overflows by checking a key container `scrollHeight <= clientHeight`.
  </action>
  <verify>
    <automated>cd e2e && npx playwright test tests/mobile-layout.spec.ts --headed --project=chromium</automated>
  </verify>
  <done>Viewport mobile layout test exists and passes on small phone sizes (no overflow assertion failure).</done>
</task>

</tasks>

<verification>
- The mobile screens use shared tokens from `design.ts`.
- Worker and job flow text is clear and includes “Submit bid” guidance.
- Playwright mobile test passed.
</verification>

<success_criteria>
- DSG-03 and DSG-04 are satisfied for mobile flows with concrete tests.
- App design quality is consistent with Phase 11 goals.
</success_criteria>

<output>
After completion, create `.planning/phases/11-app-design/11-03-SUMMARY.md`
</output>
