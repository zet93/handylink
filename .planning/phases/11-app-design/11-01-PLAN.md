---
phase: 11-app-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.css
  - frontend/src/pages/LandingPage.jsx
  - mobile/app/(client)/_layout.tsx
  - mobile/app/constants/design.ts
autonomous: true
requirements: [DSG-01, DSG-02]

must_haves:
  truths:
    - "Web and mobile share a neutral color palette with a single accent color."
    - "Typography scales and font weights are consistent across key pages."
    - "Design tokens are defined in single places and referenced by components." 
  artifacts:
    - path: "frontend/src/App.css"
      provides: "Color and typography variables"
      contains: "--accent"
    - path: "mobile/app/constants/design.ts"
      provides: "Native design tokens"
      contains: "palette, typography"
    - path: "frontend/src/pages/LandingPage.jsx"
      provides: "Landing copy style updates"
      contains: "className='text-[...]*'"
    - path: "mobile/app/(client)/_layout.tsx"
      provides: "Global mobile style container"
      contains: "backgroundColor"
  key_links:
    - from: "frontend/src/pages/JobDetailPage.jsx"
      to: "frontend/src/App.css"
      via: "className style definition"
      pattern: "var\(--accent\)"
    - from: "mobile/app/(client)/browse-workers.tsx"
      to: "mobile/app/constants/design.ts"
      via: "import {palette, typography}"
      pattern: "from '../constants/design'"
---

<objective>
Create the shared design token foundation for Phase 11 so web + mobile color and typography are consistent.

Purpose: Reduce duplicate styles, make all UI updates follow neutral palette + type scale rules roughly defined by DSG-01/DSG-02.
Output: CSS custom properties in frontend and TypeScript constants in mobile, plus a single landing page sample usage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-app-design/11-CONTEXT.md
@.planning/phases/11-app-design/11-RESEARCH.md

<interfaces>
From frontend/src/App.css:
  - existing class .counter uses var(--accent), var(--accent-bg), var(--accent-border)
From mobile/app/(client)/_layout.tsx:
  - root container returns <Slot /> but can wrap in View style
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define design tokens for web + mobile</name>
  <files>frontend/src/App.css, mobile/app/constants/design.ts</files>
  <behavior>
    - Create web CSS vars: --color-bg, --color-panel, --color-text, --color-muted, --accent, --accent-dark, --border, --shadow
    - Create web typography vars: --font-family, --font-size-base, --font-size-lg, --line-height, --font-weight-regular, --font-weight-bold
    - Create mobile constants: palette and typography objects aligned to web values.
    - Default accent is accessible blue (e.g., #1D4ED8) and neutrals are grays/white.
  </behavior>
  <action>
    Edit `frontend/src/App.css`:
      * Add `:root` block with color and typography custom properties.
      * Ensure existing `counter` style references updated to use new vars.
    Create `mobile/app/constants/design.ts`:
      * Export `palette` and `typography` objects.
    Add a small usage in `mobile/app/(client)/_layout.tsx` to apply `backgroundColor: palette.background` and `color: palette.text`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --runInBand --watchAll=false --silent 2>/dev/null || true; cd ../mobile && npm test -- --runInBand --watchAll=false --silent 2>/dev/null || true</automated>
  </verify>
  <done>Design token files exist, key variables are used in one frontend and one mobile file.</done>
</task>

<task type="auto">
  <name>Task 2: Add landing page styles and typography usage in web</name>
  <files>frontend/src/pages/LandingPage.jsx, frontend/src/App.css</files>
  <action>
    Update `LandingPage.jsx` (or existing home page) with text classes that use web design tokens.
    - Heading uses `text-4xl font-bold text-[var(--text-h)]`.
    - Subheading uses `text-lg text-[var(--text-muted)]`.
    - Buttons / cards use `bg-[var(--color-panel)] border border-[var(--border)] text-[var(--text)]`.
    Ensure responsive classes for mobile/tablet/desktop.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run test -- --to=LandingPage.test.js --runInBand || true</automated>
  </verify>
  <done>Landing page uses shared design tokens and has responsive typography classes. No style duplication in App.css.</done>
</task>

</tasks>

<verification>
- All new CSS vars exist in `frontend/src/App.css` and are referenced from `LandingPage.jsx`.
- Mobile `design.ts` is imported in `_layout.tsx` and applied as background/text color.
- `npm run test` for frontend/mobile passes.
</verification>

<success_criteria>
- Phase 11 has a solid design foundation with shared tokens and sample usage.
- Requirements DSG-01 and DSG-02 are tied to concrete artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-app-design/11-01-SUMMARY.md`
</output>
