---
phase: 16-address-nomenclators
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql
autonomous: true
requirements: [NOM-01, NOM-02, NOM-03, NOM-04]

must_haves:
  truths:
    - "004_add_county_field.sql file exists in the migrations directory"
    - "SQL adds county TEXT (nullable) to public.jobs via ADD COLUMN IF NOT EXISTS"
    - "SQL adds county TEXT (nullable) to public.profiles via ADD COLUMN IF NOT EXISTS"
    - "File follows the exact header and formatting pattern of 003_add_location_fields.sql"
  artifacts:
    - path: "backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql"
      provides: "Schema migration adding county column to jobs and profiles"
      contains: "ADD COLUMN IF NOT EXISTS county TEXT"
  key_links:
    - from: "004_add_county_field.sql"
      to: "public.jobs table"
      via: "ALTER TABLE public.jobs ADD COLUMN IF NOT EXISTS county TEXT"
      pattern: "ALTER TABLE public.jobs"
    - from: "004_add_county_field.sql"
      to: "public.profiles table"
      via: "ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS county TEXT"
      pattern: "ALTER TABLE public.profiles"
---

<objective>
Create the SQL migration file that adds the county TEXT column to both tables.

Purpose: Schema foundation for D-06 — county must exist as a column in both jobs and profiles before the backend or frontend can use it. D-07 specifies no backfill; this migration adds the column only.
Output: 004_add_county_field.sql ready to be run in the Supabase SQL editor (Plan 03 checkpoint).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-address-nomenclators/16-CONTEXT.md
@backend/HandyLink.Infrastructure/Data/Migrations/003_add_location_fields.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 004_add_county_field.sql migration</name>
  <read_first>
    - backend/HandyLink.Infrastructure/Data/Migrations/003_add_location_fields.sql (exact header and ALTER TABLE pattern to replicate)
    - .planning/phases/16-address-nomenclators/16-PATTERNS.md (004_add_county_field.sql section — exact SQL to write)
    - .planning/phases/16-address-nomenclators/16-CONTEXT.md (D-06: county TEXT nullable on jobs and profiles; D-07: no backfill)
  </read_first>
  <files>
    backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql
  </files>
  <action>
Create the file with exactly this content:

```sql
-- Migration 004: Add county field to jobs and profiles
-- Run in: Supabase Dashboard > SQL Editor > New Query

ALTER TABLE public.jobs
  ADD COLUMN IF NOT EXISTS county TEXT;

ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS county TEXT;
```

Rules:
- Use `ADD COLUMN IF NOT EXISTS` (idempotent, matches 003 pattern)
- Column is TEXT (nullable by default in PostgreSQL) — no NOT NULL, no DEFAULT (per D-07: old records stay as NULL)
- Target table for profile county is `public.profiles` NOT `public.worker_profiles` — Profile entity maps to the `profiles` table (confirmed in PATTERNS.md)
- Do NOT run `dotnet ef migrations add` — SQL script only (project non-negotiable rule)
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS county TEXT" backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql
    - File contains exactly 2 occurrences of "ADD COLUMN IF NOT EXISTS county TEXT" (one for jobs, one for profiles)
    - File contains "ALTER TABLE public.jobs"
    - File contains "ALTER TABLE public.profiles"
    - File does NOT contain "worker_profiles"
    - File does NOT contain "NOT NULL" or "DEFAULT"
    - Header comment matches: "-- Migration 004: Add county field to jobs and profiles"
  </acceptance_criteria>
  <done>004_add_county_field.sql created and ready for Supabase SQL editor execution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase DB | DDL run by developer in Supabase SQL editor; not user-facing input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | 004_add_county_field.sql | accept | DDL run by authenticated developer only; IF NOT EXISTS makes it idempotent |
| T-16-04 | Denial of Service | ALTER TABLE public.jobs | accept | ADD COLUMN on existing table is a metadata-only operation in PostgreSQL; no table lock for this statement type |
</threat_model>

<verification>
```
grep -c "ADD COLUMN IF NOT EXISTS county TEXT" backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql
```
Must output `2`.
</verification>

<success_criteria>
- 004_add_county_field.sql exists with correct content
- Two ADD COLUMN statements (jobs + profiles)
- Idempotent (IF NOT EXISTS)
- No NOT NULL constraint, no DEFAULT value
</success_criteria>

<output>
After completion, create .planning/phases/16-address-nomenclators/16-02-SUMMARY.md
</output>
