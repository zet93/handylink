---
phase: 16-address-nomenclators
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/data/ro-nomenclator.json
  - mobile/assets/ro-nomenclator.json
  - frontend/src/data/__tests__/ro-nomenclator.test.js
autonomous: true
requirements: [NOM-DATA]
must_haves:
  truths:
    - "City dropdown for Arad county shows 75 localities (not 11)"
    - "City dropdown for Arges county shows the correct count of communes + towns"
    - "All counties show both municipalities, towns (orase), AND communes (comune) — no villages/sate"
    - "Both web and mobile use the same regenerated JSON"
    - "Test NOM-06 (Bucharest sectors) still passes"
  artifacts:
    - path: "frontend/src/data/ro-nomenclator.json"
      provides: "Regenerated nomenclator with full commune coverage"
      contains: "AR-001"
    - path: "mobile/assets/ro-nomenclator.json"
      provides: "Identical copy for mobile"
    - path: "frontend/src/data/__tests__/ro-nomenclator.test.js"
      provides: "Updated test that does not hard-code city count"
  key_links:
    - from: "frontend/src/data/ro-nomenclator.json"
      to: "mobile/assets/ro-nomenclator.json"
      via: "identical file content"
      pattern: "same cities.length"
---

<objective>
Regenerate ro-nomenclator.json from the virgil-av SIRUTA source to include all municipalities, towns (orașe), AND communes (comune) for every county — not just a partial subset. The current JSON has only 11 entries for Arad but 63 for Alba, which is inconsistent and means most counties are missing hundreds of communes.

Purpose: The "Oraș / Comună" dropdown label promises communes but the data only contains a fraction of them.
Output: A complete nomenclator JSON with ~2,700+ city/commune entries (vs 359 currently), deployed to both web and mobile assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Source data URL: https://raw.githubusercontent.com/virgil-av/judet-oras-localitati-romania/master/judete.json

Classification rule (from source structure):
- Entry WITHOUT "comuna" field = top-level locality (municipality, town, or commune) → INCLUDE
- Entry WITH "comuna" field = village/sat subordinate to a commune → EXCLUDE

County ID mapping (from ro-nomenclator.json counties array):
- "auto" field in source maps to county "id" in nomenclator
- The source uses "auto" for license plate codes (AB, AR, AG, BC, etc.)
- Bucharest special case: source uses "B", nomenclator uses "B" — sectors are already correct

Current JSON location: frontend/src/data/ro-nomenclator.json (359 cities)
Mobile copy: mobile/assets/ro-nomenclator.json (identical, 359 cities — must stay in sync)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch source and generate complete nomenclator JSON</name>
  <files>frontend/src/data/ro-nomenclator.json, mobile/assets/ro-nomenclator.json</files>
  <read_first>frontend/src/data/ro-nomenclator.json</read_first>
  <action>
Write a Node.js script (inline, run with node -e or saved to a temp file) that:

1. Fetches https://raw.githubusercontent.com/virgil-av/judet-oras-localitati-romania/master/judete.json
2. For each county entry in the source array:
   - Map county "auto" code to the nomenclator county ID (they are identical — "AB", "AR", etc.)
   - Special case: Bucharest appears as "B" in both source and nomenclator counties array
3. Filter localities: include entries WHERE "comuna" field is ABSENT (undefined/missing)
4. For each included locality, create: { "id": "{COUNTY_ID}-{NNN}", "county_id": "{COUNTY_ID}", "name": "{entry.nume}" }
   - NNN is a zero-padded 3-digit sequential index within the county (001, 002, ...)
5. Keep the existing counties array unchanged (42 counties already correct)
6. Write the result to frontend/src/data/ro-nomenclator.json with structure: { "counties": [...], "cities": [...] }
7. Copy the identical file to mobile/assets/ro-nomenclator.json

The Bucharest county (id: "B") must include exactly 6 sectors. The source has București with sectors as separate locality entries. The source may list sectors as: "Sector 1", "Sector 2", ..., "Sector 6" under the "B" county. If the source does NOT include sectors by default, keep the existing 6 sector entries from the current JSON for county "B" and override the generated entries for "B" with those 6 sector entries.

Run the script and verify it produces valid JSON.
  </action>
  <verify>
    <automated>node -e "const d = require('./frontend/src/data/ro-nomenclator.json'); const ar = d.cities.filter(c=>c.county_id==='AR'); console.log('AR count:', ar.length); const b = d.cities.filter(c=>c.county_id==='B'); console.log('B sectors:', b.length); console.log('total:', d.cities.length); if(ar.length < 50) process.exit(1); if(b.length !== 6) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
- AR (Arad) city count >= 50 (was 11, should be ~75)
- B (București) city count === 6 (sectors preserved)
- Total city count > 1000 (was 359)
- mobile/assets/ro-nomenclator.json has identical cities.length as frontend version
- Every city entry has id, county_id, name fields
- Every county_id references a valid county in the counties array
  </acceptance_criteria>
  <done>Both JSON files updated with complete locality data; AR has 50+ entries; total entries exceed 1000</done>
</task>

<task type="auto">
  <name>Task 2: Update nomenclator test to reflect new counts</name>
  <files>frontend/src/data/__tests__/ro-nomenclator.test.js</files>
  <read_first>frontend/src/data/__tests__/ro-nomenclator.test.js</read_first>
  <action>
The existing test at line 5 checks `expect(nomenclator.counties).toHaveLength(42)` — keep this unchanged.

The test at line 10 checks București has exactly 6 sectors — keep this unchanged.

Add two new test cases:
1. Test that Arad county (county_id "AR") has at least 50 city entries (replaces the implicit expectation from old 11-entry data)
2. Test that total city count is greater than 1000

Do NOT add a test that hard-codes the exact total city count (it would break if the source changes).
  </action>
  <verify>
    <automated>cd c:/Users/nemet/source/repos/handylink/handylink/frontend && npx vitest run src/data/__tests__/ro-nomenclator.test.js</automated>
  </verify>
  <acceptance_criteria>
- All existing tests pass (42 counties, 6 București sectors, field structure tests)
- New test for AR count >= 50 passes
- New test for total > 1000 passes
  </acceptance_criteria>
  <done>All nomenclator tests pass with updated expectations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CDN → build script | Raw JSON fetched from GitHub at build/generate time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-16-07-01 | Tampering | judete.json fetch | accept | Data is static Romanian geographic data; no PII; worst case is incorrect city names. File is committed to repo after generation so supply-chain risk is a one-time event. |
</threat_model>

<verification>
After both tasks complete:
- node -e check confirms AR >= 50 entries
- node -e check confirms total > 1000 entries
- mobile/assets/ro-nomenclator.json matches frontend version (same cities.length)
- Vitest nomenclator tests all pass
</verification>

<success_criteria>
Both JSON files regenerated with complete SIRUTA data. Arad county has 50+ entries. Total entries exceed 1000. All tests pass. Mobile and web JSON are identical.
</success_criteria>

<output>
After completion, create `.planning/phases/16-address-nomenclators/16-07-SUMMARY.md`
</output>
