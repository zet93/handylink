---
phase: 16-address-nomenclators
plan: 04
type: execute
wave: 2
depends_on: [16-02]
files_modified: []
autonomous: false
requirements: [NOM-01, NOM-03]

must_haves:
  truths:
    - "county column exists in public.jobs table in the Supabase database"
    - "county column exists in public.profiles table in the Supabase database"
    - "county column is nullable TEXT (no NOT NULL constraint)"
  artifacts:
    - path: "backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql"
      provides: "SQL that was executed"
      contains: "ADD COLUMN IF NOT EXISTS county TEXT"
  key_links:
    - from: "public.jobs.county"
      to: "Job.County C# property"
      via: "EF Core column mapping (conventional name match)"
      pattern: "County"
    - from: "public.profiles.county"
      to: "Profile.County C# property"
      via: "EF Core column mapping (conventional name match)"
      pattern: "County"
---

<objective>
[BLOCKING] Run the 004_add_county_field.sql migration in the Supabase SQL editor to add the county column to the live database.

Purpose: The backend cannot persist County values and tests cannot verify end-to-end storage without the column existing in Supabase. This is a manual step — Claude cannot execute SQL against the hosted Supabase database directly.
Output: county TEXT column present in public.jobs and public.profiles.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-address-nomenclators/16-CONTEXT.md
@backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: [BLOCKING] Run 004_add_county_field.sql in Supabase SQL editor</name>
  <read_first>
    - backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql (copy the SQL from this file)
  </read_first>
  <what-built>
    Plan 02 created the SQL migration file at backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql.
    This file adds `county TEXT` (nullable) to public.jobs and public.profiles.
  </what-built>
  <how-to-verify>
    1. Open the Supabase Dashboard for your HandyLink project.
    2. Go to: SQL Editor → New Query.
    3. Paste the contents of backend/HandyLink.Infrastructure/Data/Migrations/004_add_county_field.sql:
       ```sql
       ALTER TABLE public.jobs
         ADD COLUMN IF NOT EXISTS county TEXT;

       ALTER TABLE public.profiles
         ADD COLUMN IF NOT EXISTS county TEXT;
       ```
    4. Click Run.
    5. Verify success: go to Table Editor → jobs → columns. Confirm `county` column exists with type `text`.
    6. Repeat for Table Editor → profiles → columns. Confirm `county` column exists with type `text`.
  </how-to-verify>
  <resume-signal>Type "schema pushed" once both columns are confirmed in the Supabase Table Editor</resume-signal>
  <acceptance_criteria>
    - public.jobs table has a column named "county" with type "text" (nullable)
    - public.profiles table has a column named "county" with type "text" (nullable)
    - SQL ran without errors in the Supabase SQL editor
  </acceptance_criteria>
  <done>county column exists in both tables in the live Supabase database</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Supabase SQL editor | DDL executed by authenticated developer against managed database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Tampering | ALTER TABLE DDL | accept | DDL run by authenticated developer; IF NOT EXISTS makes it safe to re-run |
</threat_model>

<verification>
Supabase Table Editor shows:
- public.jobs.county — type: text, nullable: true
- public.profiles.county — type: text, nullable: true
</verification>

<success_criteria>
- county column confirmed in public.jobs
- county column confirmed in public.profiles
- No existing data modified (old records have NULL for county per D-07)
</success_criteria>

<output>
After completion, create .planning/phases/16-address-nomenclators/16-04-SUMMARY.md
</output>
