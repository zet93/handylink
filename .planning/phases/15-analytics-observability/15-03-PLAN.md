---
phase: 15-analytics-observability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/backup.yml
  - OPERATIONS.md
autonomous: true
requirements:
  - OPS-02
  - OPS-03

must_haves:
  truths:
    - "A GitHub Actions cron job runs daily at 02:00 UTC and dumps the Supabase database"
    - "The backup artifact is retained for 30 days"
    - "A dependency update process is documented at the repo root"
  artifacts:
    - path: ".github/workflows/backup.yml"
      provides: "Supabase database backup cron workflow"
      contains: "cron: '0 2 * * *'"
    - path: "OPERATIONS.md"
      provides: "Dependency update process documentation"
      contains: "security patches"
  key_links:
    - from: ".github/workflows/backup.yml"
      to: "GitHub artifact storage"
      via: "actions/upload-artifact@v4 with retention-days: 30"
      pattern: "retention-days: 30"
    - from: ".github/workflows/backup.yml"
      to: "Supabase database"
      via: "supabase db dump --db-url ${{ secrets.SUPABASE_DB_URL }}"
      pattern: "SUPABASE_DB_URL"
---

<objective>
Create the automated Supabase backup GitHub Actions workflow (OPS-02) and document the dependency update process (OPS-03). Both are independent of the analytics implementation — pure infrastructure and documentation work.

Purpose: OPS-02 gives a 30-day backup history independent of Supabase's free-tier retention. OPS-03 gives the solo developer a clear process for keeping dependencies patched.
Output: .github/workflows/backup.yml, OPERATIONS.md at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-analytics-observability/15-RESEARCH.md
@.planning/phases/15-analytics-observability/15-CONTEXT.md

<interfaces>
<!-- Analog workflow structure from .github/workflows/backend-ci.yml -->

```yaml
name: Backend CI
on:
  push:
    branches: [main, development]
    paths: ['backend/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: backend/**/*.trx
```

Secret reference pattern: `${{ secrets.RENDER_DEPLOY_HOOK_URL }}` → apply as `${{ secrets.SUPABASE_DB_URL }}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase backup GitHub Actions workflow</name>
  <files>.github/workflows/backup.yml</files>
  <read_first>
    - .github/workflows/backend-ci.yml — read for workflow structure, action versions, secret syntax, artifact upload pattern
  </read_first>
  <action>
Create `.github/workflows/backup.yml` with exactly this content:

```yaml
name: Supabase Backup

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: supabase/setup-cli@v1

      - name: Dump database
        run: supabase db dump --db-url "${{ secrets.SUPABASE_DB_URL }}" -f backup.sql

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: supabase-backup-${{ github.run_id }}
          path: backup.sql
          retention-days: 30
```

**Notes for the executor:**
- `workflow_dispatch` allows manual runs from GitHub Actions UI — useful to verify the workflow before the first scheduled run.
- `SUPABASE_DB_URL` must be added as a GitHub repository secret. The value is found at: Supabase dashboard → Settings → Database → Connection string (URI format, e.g. `postgresql://postgres.xxx:password@aws-0-eu-central-1.pooler.supabase.com:6543/postgres`). This secret must be set manually by the developer.
- `supabase/setup-cli@v1` installs the Supabase CLI — no `actions/checkout@v4` needed because there's no source code to check out; the dump is a direct DB connection.
- `retention-days: 30` gives 30-day history via GitHub artifact storage, independent of Supabase's own free-tier backup retention.

After creating the file, verify it is valid YAML:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/backup.yml'))" 2>&1
```
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/backup.yml')); print('valid')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/backup.yml exists
    - File contains `cron: '0 2 * * *'`
    - File contains `supabase db dump`
    - File contains `secrets.SUPABASE_DB_URL`
    - File contains `retention-days: 30`
    - File contains `workflow_dispatch`
    - YAML is syntactically valid (python3 yaml.safe_load exits 0)
  </acceptance_criteria>
  <done>backup.yml created and syntactically valid. Workflow will run daily at 02:00 UTC once SUPABASE_DB_URL secret is set in GitHub.</done>
</task>

<task type="auto">
  <name>Task 2: Create OPERATIONS.md dependency update process</name>
  <files>OPERATIONS.md</files>
  <read_first>
    - CLAUDE.md — check if any existing ops documentation exists before creating
  </read_first>
  <action>
Create `OPERATIONS.md` at the repo root (alongside CLAUDE.md). This documents the dependency update process per D-17. Content must be a checklist format covering: monthly check for major updates, immediate application of security patches, branch-then-merge workflow for updates.

```markdown
# HandyLink Operations

## Dependency Update Process

### Monthly Checks (first Monday of each month)

- [ ] **Backend (.NET / NuGet):** `dotnet list backend/ package --outdated`
  - Review major version bumps for breaking changes
  - Apply minor/patch updates immediately: edit `.csproj` version → `dotnet restore backend/` → `dotnet test backend/`
  - For major bumps: create a branch, test, merge via PR

- [ ] **Frontend (npm):** `npm outdated` (from `frontend/`)
  - Apply minor/patch: `npm update` → `npm run test` → `npm run build`
  - For major bumps (e.g. React, Vite, Tailwind): create a branch, test, merge via PR

- [ ] **Mobile (Expo):** `npx expo install --check` (from `mobile/`)
  - Expo manages SDK peer dependencies — always use `npx expo install <pkg>` not `npm install`
  - For Expo SDK major upgrades: follow the official upgrade guide at https://docs.expo.dev/workflow/upgrading-expo-sdk-walkthrough/

### Security Patches (apply immediately on disclosure)

- Monitor **GitHub Dependabot alerts** on the repository
- Monitor **npm audit:** `npm audit --audit-level=high` (frontend + mobile)
- Monitor **NuGet advisories:** https://github.com/advisories (filter: NuGet)
- Apply security patches on a dedicated branch: `git checkout -b security/patch-<pkg>-<version>`
- Run full test suite before merging: `dotnet test backend/ && npm run test --prefix frontend`

### Update Workflow

1. Create a branch: `git checkout -b chore/update-deps-<YYYY-MM>`
2. Apply updates per the checklist above
3. Run full test suite: `dotnet test backend/ && npm run test --prefix frontend`
4. Build frontend: `npm run build --prefix frontend`
5. Smoke-test locally: `dotnet run --project backend/HandyLink.API` + `npm run dev --prefix frontend`
6. Merge via PR with description of what was updated and why

### Key Commands

| Command | Purpose |
|---------|---------|
| `dotnet list backend/ package --outdated` | Show outdated NuGet packages |
| `npm outdated` (from `frontend/`) | Show outdated npm packages |
| `npx expo install --check` (from `mobile/`) | Show Expo SDK dependency mismatches |
| `npm audit --audit-level=high` | Show high-severity npm vulnerabilities |
| `dotnet test backend/` | Run backend test suite |
| `npm run test --prefix frontend` | Run frontend test suite |

### External Service Updates

- **Supabase:** Watch for deprecation notices in the Supabase dashboard. `@supabase/supabase-js` follows Supabase API versions — update together.
- **Stripe:** Breaking changes are announced via the Stripe changelog. Pin the `Stripe.net` and `@stripe/stripe-js` versions; upgrade intentionally.
- **PostHog:** Auto-updates are safe for minor versions. Check the PostHog changelog before major upgrades: https://posthog.com/changelog
- **Sentry:** Follow the Sentry migration guide for SDK major versions.
```
  </action>
  <verify>
    <automated>grep -c "security patches" OPERATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - OPERATIONS.md exists at the repo root
    - File contains `security patches` (case-insensitive)
    - File contains `npm audit`
    - File contains `dotnet list backend/ package --outdated`
    - File contains `npx expo install --check`
    - File contains a checklist format (lines starting with `- [ ]`)
  </acceptance_criteria>
  <done>OPERATIONS.md created at repo root with monthly check process, immediate security patch process, update workflow, and key commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Supabase DB | DB connection string used in CI — transmitted via encrypted GitHub secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Information Disclosure | SUPABASE_DB_URL in GitHub secret | mitigate | Use GitHub encrypted repository secret — never commit the value. backup.yml references it via `${{ secrets.SUPABASE_DB_URL }}` which GitHub masks in logs. |
| T-15-09 | Information Disclosure | backup.sql artifact | accept | GitHub artifacts are private to repo collaborators; for a solo-dev repo this is acceptable for beta |
| T-15-10 | Denial of Service | Backup workflow running on every push | accept | Workflow uses `schedule` + `workflow_dispatch` only — no push trigger, no risk of runaway billing |
</threat_model>

<verification>
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/backup.yml')); print('valid')"
grep "cron: '0 2 \* \* \*'" .github/workflows/backup.yml
grep "retention-days: 30" .github/workflows/backup.yml
grep "SUPABASE_DB_URL" .github/workflows/backup.yml
grep "security patches" OPERATIONS.md
```
</verification>

<success_criteria>
- .github/workflows/backup.yml exists, valid YAML, schedules daily at 02:00 UTC, dumps to backup.sql, uploads artifact with 30-day retention
- OPERATIONS.md exists at repo root with monthly check checklist, security patch workflow, key commands table
- Developer knows to add SUPABASE_DB_URL as a GitHub repository secret (documented in plan notes above)
</success_criteria>

<output>
After completion, create `.planning/phases/15-analytics-observability/15-03-SUMMARY.md`
</output>
