---
phase: 17-ai-slop-frontend-improvements
plan: 04
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - frontend/src/pages/PostJobPage.jsx
  - frontend/src/components/NavBar.jsx
autonomous: true
requirements:
  - D-04
  - D-13

must_haves:
  truths:
    - "PostJobPage category select options show Romanian labels; option values remain raw enum strings"
    - "NavBar notification bell is a Lucide Bell SVG icon, not the 🔔 emoji"
    - "NavBar active-state styling is applied to the current route link"
  artifacts:
    - path: "frontend/src/pages/PostJobPage.jsx"
      provides: "Post job form with Romanian category labels in the select"
    - path: "frontend/src/components/NavBar.jsx"
      provides: "NavBar with Lucide Bell icon and active link states"
  key_links:
    - from: "PostJobPage.jsx category select"
      to: "frontend/src/constants/categories.js"
      via: "import { getCategoryLabel, CATEGORY_KEYS }"
    - from: "NavBar.jsx NotificationBell"
      to: "lucide-react Bell"
      via: "import { Bell } from 'lucide-react'"
---

<objective>
Apply Romanian category labels to PostJobPage and polish the NavBar: replace the 🔔 emoji with a Lucide Bell icon and add active-link styling.

Purpose: PostJobPage is the job creation entry point — raw enum values in the dropdown confuse Romanian users. The emoji bell is jarring in an otherwise clean nav. Active states give users orientation.

Output: Two updated files — PostJobPage.jsx and NavBar.jsx.
</objective>

<execution_context>
@C:/Users/nemet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nemet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-slop-frontend-improvements/17-CONTEXT.md
@.planning/phases/17-ai-slop-frontend-improvements/17-01-SUMMARY.md

<interfaces>
From frontend/src/constants/categories.js (plan 17-01):
```js
export const CATEGORY_KEYS = ['electrical','plumbing','painting','carpentry','furniture_assembly','cleaning','general','other'];
export function getCategoryLabel(raw) { return CATEGORY_LABELS[raw] ?? raw; }
```

Current PostJobPage.jsx state:
- Line 11: `const CATEGORIES = ['electrical', 'plumbing', 'painting', 'carpentry', 'furniture_assembly', 'cleaning', 'general', 'other'];`
- Line 17: `category: z.enum(CATEGORIES)` — this Zod enum must keep using the raw values array
- Line 103-105: select options: `{c.replace('_', ' ')}`

Current NavBar.jsx state:
- Line 46: `🔔` emoji in the button inside NotificationBell
- Uses `Link` from react-router-dom — can use `NavLink` for active states
- No active state applied to any link currently
- lucide-react is available after plan 17-02 installs it in frontend/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T07: Romanian labels in PostJobPage category select</name>
  <files>frontend/src/pages/PostJobPage.jsx</files>
  <action>
**In `frontend/src/pages/PostJobPage.jsx`:**

1. Add import:
```jsx
import { getCategoryLabel, CATEGORY_KEYS } from '../constants/categories';
```

2. Remove the local `CATEGORIES` array at line 11.

3. The Zod schema `category: z.enum(CATEGORIES)` must be updated to use `CATEGORY_KEYS`:
```js
category: z.enum(CATEGORY_KEYS),
```
`CATEGORY_KEYS` is a plain array of strings, so `z.enum()` accepts it the same way.

4. Replace the select options:
- Old: `{CATEGORIES.map(c => (<option key={c} value={c}>{c.replace('_', ' ')}</option>))}`
- New: `{CATEGORY_KEYS.map(c => (<option key={c} value={c}>{getCategoryLabel(c)}</option>))}`

The `value` attribute stays as the raw enum string — only the displayed label changes.
  </action>
  <verify>
    <automated>grep -n "replace.*_" frontend/src/pages/PostJobPage.jsx; echo "---replace('_') refs (should be 0)"; grep -n "getCategoryLabel" frontend/src/pages/PostJobPage.jsx</automated>
  </verify>
  <done>
    No `replace('_', ' ')` in PostJobPage.jsx. `getCategoryLabel` is imported and used. Option values remain raw enum strings. `z.enum(CATEGORY_KEYS)` compiles without errors.
  </done>
</task>

<task type="auto">
  <name>T08: NavBar polish — Bell icon, active link states</name>
  <files>frontend/src/components/NavBar.jsx</files>
  <action>
**In `frontend/src/components/NavBar.jsx`:**

1. Add imports:
```jsx
import { Bell } from 'lucide-react';
import { NavLink } from 'react-router-dom';
```
Keep the existing `Link` import — `Link` is still used for the logo and auth buttons. Use `NavLink` for the navigation links that should receive active styling.

2. Replace the 🔔 emoji in the `NotificationBell` button (line 46 area):
```jsx
<Bell size={20} style={{ color: 'var(--color-text)' }} />
```
Keep the `unreadCount` badge `<span>` exactly as-is — only the emoji changes.

3. Replace nav `Link` elements with `NavLink` for route links only (not the logo, not auth buttons):

For both the unauthenticated and authenticated nav variants, replace `Link` with `NavLink` for the route links (Jobs, Browse Workers, Browse Jobs, Post a Job, My Jobs). Apply active style via the `NavLink` className callback:

```jsx
<NavLink
  to="/jobs"
  className={({ isActive }) =>
    `text-sm hover:text-blue-600 ${isActive ? 'text-blue-600 font-semibold' : 'text-gray-700'}`
  }
>
  Jobs
</NavLink>
```

Apply the same pattern to all nav route links. The logo `Link to="/"` and auth buttons (`Log in`, `Register`, `Sign out`, `Profile`) stay as `Link` or `button` — do not convert them to `NavLink`.
  </action>
  <verify>
    <automated>grep -n "🔔" frontend/src/components/NavBar.jsx; echo "---bell emoji (should be 0)"; grep -n "NavLink" frontend/src/components/NavBar.jsx</automated>
  </verify>
  <done>
    No 🔔 character in NavBar.jsx. `NavLink` is used for at least 2 route links. `Bell` icon from lucide-react renders in place of the emoji. Build passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | PostJobPage submits category value to API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | PostJobPage category field | accept | Option values remain raw enum strings; Zod schema still validates against CATEGORY_KEYS before submit; API independently validates the enum on the backend |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- No `replace('_', ' ')` in PostJobPage.jsx or NavBar.jsx
- No 🔔 emoji in NavBar.jsx
- `NavLink` used for nav route links in NavBar.jsx
- `Bell` icon import present in NavBar.jsx
</verification>

<success_criteria>
Build passes. PostJobPage dropdown shows Romanian labels while submitting raw enum values. NavBar bell is a crisp SVG icon. Active nav links are visually distinct (blue + semibold).
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-slop-frontend-improvements/17-04-SUMMARY.md`
</output>
