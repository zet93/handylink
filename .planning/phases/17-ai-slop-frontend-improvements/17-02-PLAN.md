---
phase: 17-ai-slop-frontend-improvements
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - frontend/src/pages/LandingPage.jsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-07
  - D-08
  - D-09

must_haves:
  truths:
    - "Hero headline references Romania, not 'globally'"
    - "Hero subtext feels local and direct, not generic SaaS copy"
    - "Category grid shows Lucide SVG icons instead of emoji"
    - "Category grid shows Romanian labels (Electrician, Instalator, etc.)"
    - "Both CTAs still link to /register"
    - "Recent jobs section remains below how-it-works with loading and empty states"
  artifacts:
    - path: "frontend/src/pages/LandingPage.jsx"
      provides: "Updated landing page with Romanian copy, Lucide icons, Romanian labels, loading/empty states"
  key_links:
    - from: "LandingPage.jsx CATEGORIES array"
      to: "frontend/src/constants/categories.js"
      via: "import { getCategoryLabel, CATEGORY_KEYS } from '../constants/categories'"
    - from: "LandingPage.jsx category grid"
      to: "lucide-react icons"
      via: "import { Zap, Wrench, ... } from 'lucide-react'"
---

<objective>
Rewrite the LandingPage hero copy for the Romanian market, replace emoji with Lucide icons in the category grid, apply Romanian labels, and add proper loading/empty states to the recent jobs section.

Purpose: The landing page is the first impression. "Globally" and emoji in a trade marketplace targeting Romania undermine credibility. This task eliminates all three slop signals in one file.

Output: Updated `frontend/src/pages/LandingPage.jsx` with Romanian hero copy, Lucide icons, Romanian category labels, and loading/empty states.
</objective>

<execution_context>
@C:/Users/nemet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nemet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-slop-frontend-improvements/17-CONTEXT.md
@.planning/phases/17-ai-slop-frontend-improvements/17-01-SUMMARY.md

<interfaces>
From frontend/src/constants/categories.js (created in plan 17-01):
```js
export const CATEGORY_LABELS = {
  electrical: 'Electrician', plumbing: 'Instalator', painting: 'Zugrav',
  carpentry: 'Tâmplărie', furniture_assembly: 'Mobilă', cleaning: 'Curățenie',
  general: 'General', other: 'Altele',
};
export const CATEGORY_KEYS = Object.keys(CATEGORY_LABELS); // 8 items
export function getCategoryLabel(raw) { return CATEGORY_LABELS[raw] ?? raw; }
```

Current LandingPage.jsx state (key sections):
- Line 6-13: CATEGORIES array with emoji + English label
- Line 38: Hero h1: "Find trusted tradespeople near you"
- Line 39-41: Hero p: "...connects clients with skilled workers globally."
- Line 56-65: Category grid maps over CATEGORIES, renders `{emoji}` in a span
- Line 28-33: useQuery for recentJobs (isLoading, isError, data) — data already fetched
- No recent jobs section rendered (recentJobs query result is unused in JSX)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>T03: Install lucide-react</name>
  <files>frontend/package.json</files>
  <action>
Run `npm install lucide-react` from the `frontend/` directory. This adds `lucide-react` to `dependencies` in `frontend/package.json`. Do not pin a version — use the latest.

Command: `cd frontend && npm install lucide-react`
  </action>
  <verify>
    <automated>node -e "require('C:/Users/nemet/source/repos/handylink/handylink/frontend/node_modules/lucide-react')" && echo "OK"</automated>
  </verify>
  <done>
    `lucide-react` appears in `frontend/package.json` dependencies. `frontend/node_modules/lucide-react` exists.
  </done>
</task>

<task type="auto">
  <name>T04: Rewrite LandingPage — copy, icons, labels, states</name>
  <files>frontend/src/pages/LandingPage.jsx</files>
  <action>
Rewrite `frontend/src/pages/LandingPage.jsx` with these exact changes:

**1. Imports — replace the old CATEGORIES import and add:**
```jsx
import { Zap, Wrench, Paintbrush, Hammer, Armchair, Sparkles, Home, MoreHorizontal } from 'lucide-react';
import { getCategoryLabel } from '../constants/categories';
```

**2. CATEGORIES array — replace the emoji array with icon components (per D-08):**
```jsx
const CATEGORIES = [
  { key: 'electrical',       Icon: Zap },
  { key: 'plumbing',         Icon: Wrench },
  { key: 'painting',         Icon: Paintbrush },
  { key: 'carpentry',        Icon: Hammer },
  { key: 'furniture_assembly', Icon: Armchair },
  { key: 'cleaning',         Icon: Sparkles },
  { key: 'general',          Icon: Home },
  { key: 'other',            Icon: MoreHorizontal },
];
```

**3. Hero section — replace the h1 and p text (per D-01):**
- h1: `Găsești meșterul potrivit în orașul tău`
- p: `Postează o lucrare sau găsește clienți — HandyLink conectează meșteri de încredere cu oamenii care au nevoie de ei.`
- Keep the two CTA links to `/register` unchanged (per D-02).

**4. Category grid — update the map to use Icon component and getCategoryLabel (per D-04, D-07, D-08):**
```jsx
{CATEGORIES.map(({ key, Icon }) => (
  <Link
    key={key}
    to="/jobs"
    className="flex flex-col items-center gap-2 p-4 border rounded-xl hover:border-blue-400 hover:bg-blue-50 text-center"
    style={{ borderColor: 'var(--border)', backgroundColor: 'var(--color-panel)', color: 'var(--color-text)' }}
  >
    <Icon size={28} style={{ color: 'var(--accent)' }} />
    <span className="text-sm font-medium" style={{ color: 'var(--color-muted)' }}>{getCategoryLabel(key)}</span>
  </Link>
))}
```

**5. Recent jobs section — add below the how-it-works section, before the footer (per D-03):**
```jsx
<section className="py-16 px-6 max-w-5xl mx-auto">
  <h2 className="text-2xl font-bold text-center mb-10">Lucrări recente</h2>
  {jobsLoading ? (
    <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
      {[...Array(3)].map((_, i) => (
        <div key={i} className="border rounded-xl p-4 animate-pulse" style={{ backgroundColor: 'var(--color-panel)' }}>
          <div className="h-4 rounded mb-3" style={{ backgroundColor: 'var(--border)', width: '60%' }} />
          <div className="h-3 rounded mb-2" style={{ backgroundColor: 'var(--border)', width: '80%' }} />
          <div className="h-3 rounded" style={{ backgroundColor: 'var(--border)', width: '40%' }} />
        </div>
      ))}
    </div>
  ) : !recentJobs?.length ? (
    <p className="text-center text-sm" style={{ color: 'var(--color-muted)' }}>Nu există lucrări disponibile momentan.</p>
  ) : (
    <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
      {recentJobs.map(job => <JobCard key={job.id} job={job} />)}
    </div>
  )}
</section>
```

The `recentJobs` and `jobsLoading` variables are already declared via the existing `useQuery` hook at the top of the component — no new query needed.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` completes without errors. Landing page renders with Romanian hero copy, SVG icons in the category grid (not emoji), Romanian category labels, and a recent jobs section with skeleton loading placeholders.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→recentJobs display | Job data from server rendered on public landing page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Information Disclosure | recentJobs section | accept | Only public job data (title, category, city) rendered — no PII, same data available on /jobs page |
</threat_model>

<verification>
- Build passes: `cd frontend && npm run build` exits 0
- `LandingPage.jsx` imports from `lucide-react` and `../constants/categories`
- No emoji characters remain in the category grid JSX
- Hero h1 does not contain "globally"
- `recentJobs` is rendered in JSX (no longer unused)
</verification>

<success_criteria>
Landing page builds cleanly. Romanian hero copy is present. Category grid shows 8 Lucide icons with Romanian labels. Recent jobs section shows skeletons while loading, a message when empty, and JobCard components when data is available.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-slop-frontend-improvements/17-02-SUMMARY.md`
</output>
