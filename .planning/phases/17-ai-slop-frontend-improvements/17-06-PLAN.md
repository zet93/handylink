---
phase: 17-ai-slop-frontend-improvements
plan: 06
type: execute
wave: 3
depends_on:
  - 17-02
  - 17-03
  - 17-04
  - 17-05
autonomous: false
requirements:
  - D-01
  - D-04
  - D-05
  - D-07
  - D-10
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "Web build succeeds cleanly"
    - "Landing page hero shows Romanian copy with Lucide icons in category grid"
    - "JobsPage has no Country filter and shows skeleton + empty state"
    - "JobCard, JobDetailPage, PostJobPage all show Romanian category labels"
    - "NavBar bell is a Lucide icon with active-link styling"
    - "Mobile screens show Romanian category labels"
  artifacts:
    - path: "frontend/src/constants/categories.js"
      provides: "Shared category constants (created plan 17-01)"
    - path: "mobile/constants/categories.ts"
      provides: "Shared category constants (created plan 17-01)"
  key_links: []
---

<objective>
Human verification pass across all changed surfaces. Confirm the build is clean, visuals look correct, and no raw enum strings or emoji slop remain visible in the UI.

Purpose: Plans 17-02 through 17-05 touched 9 files across web and mobile. A final human review catches visual regressions that automated grep checks cannot.

Output: Approved or issue list to address.
</objective>

<execution_context>
@C:/Users/nemet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nemet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-slop-frontend-improvements/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>T11: Final build verification and automated grep checks</name>
  <files></files>
  <action>
Run the following checks in sequence:

**1. Web build:**
```bash
cd frontend && npm run build
```
Must exit 0. Surface any TypeScript/ESLint errors.

**2. Category slop grep — confirm no raw `.replace('_', ' ')` on category values remain:**
```bash
grep -rn "\.replace.*_.*' '" frontend/src/pages/ frontend/src/components/ mobile/app/
```
Expected: zero matches (or only matches unrelated to category display).

**3. Emoji grep — confirm 🔔 is gone:**
```bash
grep -rn "🔔" frontend/src/
```
Expected: zero matches.

**4. Country filter grep — confirm removed from JobsPage:**
```bash
grep -n "country" frontend/src/pages/JobsPage.jsx
```
Expected: zero matches.

**5. Constants consistency check:**
```bash
grep -n "getCategoryLabel" \
  frontend/src/pages/LandingPage.jsx \
  frontend/src/pages/JobsPage.jsx \
  frontend/src/components/JobCard.jsx \
  frontend/src/pages/JobDetailPage.jsx \
  frontend/src/pages/PostJobPage.jsx \
  frontend/src/components/NavBar.jsx \
  mobile/app/\(public\)/browse.tsx \
  mobile/app/\(public\)/job-detail.tsx \
  mobile/app/\(client\)/post-job.tsx \
  mobile/app/\(worker\)/browse.tsx
```
Expected: at least one match per file (except NavBar.jsx, which doesn't display categories).

Report any failures before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
    All five grep checks return expected results. Web build exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 17-01 through 17-05 applied all phase changes:
    - Shared category constants created (web + mobile)
    - LandingPage: Romanian hero copy, Lucide icons, Romanian labels, recent jobs with loading/empty states
    - JobsPage: country filter removed, Romanian labels, skeleton loading, empty state
    - JobCard + JobDetailPage: Romanian category labels
    - PostJobPage: Romanian category labels in select
    - NavBar: Lucide Bell icon, active-link styling
    - All 4 mobile screens: Romanian category labels
  </what-built>
  <how-to-verify>
    1. Start web: `cd frontend && npm run dev` — visit http://localhost:5173

    **Landing page (http://localhost:5173/):**
    - Hero headline is in Romanian and does NOT contain "globally"
    - Category grid shows 8 SVG icons (not emoji), with Romanian labels (Electrician, Instalator, Zugrav, etc.)
    - Recent jobs section shows skeletons while loading, then job cards (or Romanian empty message)

    **Jobs page (http://localhost:5173/jobs):**
    - Sidebar has Category, City, Status filters — NO Country filter
    - Category dropdown shows Romanian options
    - While loading: skeleton placeholder cards visible
    - If no results: Briefcase icon + Romanian message shown

    **NavBar (visible on all pages when logged in):**
    - Notification bell is an SVG icon, not 🔔
    - Active page link is blue and semibold
    - Other nav links are gray

    **Post a Job (http://localhost:5173/post-job — requires login):**
    - Category select shows Romanian options (Electrician, Instalator, etc.)

    **Mobile (optional, run `npx expo start` from `mobile/`):**
    - Browse screen: job cards show Romanian category label
    - Worker browse: category chip labels are in Romanian
    - Post job: category picker shows Romanian labels
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→visual review | Human confirms no raw enum values visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Information Disclosure | visual review checkpoint | accept | No sensitive data involved; checkpoint verifies UI correctness only |
</threat_model>

<verification>
- `npm run build` exits 0
- No `.replace('_', ' ')` on category fields in any modified file
- No 🔔 in NavBar.jsx
- No `country` in JobsPage filters
- Human approved all visual surfaces
</verification>

<success_criteria>
Human has confirmed: Romanian copy on landing page, Lucide icons in category grid, Romanian labels on all user-visible category fields across web and mobile, NavBar bell replaced, country filter gone, loading/empty states present.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-slop-frontend-improvements/17-06-SUMMARY.md`
</output>
