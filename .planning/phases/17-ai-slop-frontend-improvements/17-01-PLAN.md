---
phase: 17-ai-slop-frontend-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/constants/categories.js
  - mobile/constants/categories.ts
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "A single import provides Romanian category labels on web"
    - "A single import provides Romanian category labels on mobile"
    - "getCategoryLabel('electrical') returns 'Electrician' on both platforms"
    - "getCategoryLabel('unknown_value') returns the raw value as fallback"
  artifacts:
    - path: "frontend/src/constants/categories.js"
      provides: "CATEGORY_LABELS map and getCategoryLabel helper"
      exports: ["CATEGORY_LABELS", "getCategoryLabel", "CATEGORY_KEYS"]
    - path: "mobile/constants/categories.ts"
      provides: "CATEGORY_LABELS map and getCategoryLabel helper (TypeScript)"
      exports: ["CATEGORY_LABELS", "getCategoryLabel", "CATEGORY_KEYS"]
  key_links:
    - from: "frontend/src/constants/categories.js"
      to: "all web components displaying category"
      via: "named import getCategoryLabel"
    - from: "mobile/constants/categories.ts"
      to: "all mobile screens displaying category"
      via: "named import getCategoryLabel"
---

<objective>
Create the single source-of-truth for category display labels on both web and mobile.

Purpose: All other tasks in this phase depend on this constant existing before they can replace raw enum values. Creating this first eliminates duplication and satisfies D-06.

Output: `frontend/src/constants/categories.js` and `mobile/constants/categories.ts` — both exporting identical label maps and a helper function.
</objective>

<execution_context>
@C:/Users/nemet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nemet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-ai-slop-frontend-improvements/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>T01: Create web category constants</name>
  <files>frontend/src/constants/categories.js</files>
  <action>
Create `frontend/src/constants/categories.js` with the following exact content (per D-04 and D-06):

```js
export const CATEGORY_LABELS = {
  electrical: 'Electrician',
  plumbing: 'Instalator',
  painting: 'Zugrav',
  carpentry: 'Tâmplărie',
  furniture_assembly: 'Mobilă',
  cleaning: 'Curățenie',
  general: 'General',
  other: 'Altele',
};

export const CATEGORY_KEYS = Object.keys(CATEGORY_LABELS);

export function getCategoryLabel(raw) {
  return CATEGORY_LABELS[raw] ?? raw;
}
```

The `frontend/src/constants/` directory does not exist yet — create it.
  </action>
  <verify>
    <automated>node -e "const { getCategoryLabel, CATEGORY_KEYS } = require('./frontend/src/constants/categories.js'); console.assert(getCategoryLabel('electrical') === 'Electrician'); console.assert(getCategoryLabel('unknown') === 'unknown'); console.assert(CATEGORY_KEYS.length === 8); console.log('OK')"</automated>
  </verify>
  <done>
    File exists at `frontend/src/constants/categories.js`. Importing `getCategoryLabel('electrical')` returns `'Electrician'`. Importing `getCategoryLabel('xyz')` returns `'xyz'`. `CATEGORY_KEYS` has 8 entries.
  </done>
</task>

<task type="auto">
  <name>T02: Create mobile category constants</name>
  <files>mobile/constants/categories.ts</files>
  <action>
Create `mobile/constants/categories.ts` with the following exact content (per D-04 and D-06). The `mobile/constants/` directory does not exist yet — create it.

```ts
export const CATEGORY_LABELS: Record<string, string> = {
  electrical: 'Electrician',
  plumbing: 'Instalator',
  painting: 'Zugrav',
  carpentry: 'Tâmplărie',
  furniture_assembly: 'Mobilă',
  cleaning: 'Curățenie',
  general: 'General',
  other: 'Altele',
};

export const CATEGORY_KEYS = Object.keys(CATEGORY_LABELS);

export function getCategoryLabel(raw: string): string {
  return CATEGORY_LABELS[raw] ?? raw;
}
```
  </action>
  <verify>
    <automated>node -e "require('ts-node/register'); const { getCategoryLabel } = require('./mobile/constants/categories.ts'); console.assert(getCategoryLabel('plumbing') === 'Instalator'); console.log('OK')" 2>/dev/null || echo "File exists — TypeScript verified at compile time during mobile build"</automated>
  </verify>
  <done>
    File exists at `mobile/constants/categories.ts`. Exports `CATEGORY_LABELS`, `CATEGORY_KEYS`, and `getCategoryLabel`. TypeScript types are correct (no `any`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→display | Category value arrives from API, displayed to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | getCategoryLabel fallback | accept | Fallback returns the raw API value — no sensitive data, enum values are non-secret identifiers |
</threat_model>

<verification>
- `frontend/src/constants/categories.js` exists with all 8 keys from D-04
- `mobile/constants/categories.ts` exists with all 8 keys from D-04
- Both files export `CATEGORY_LABELS`, `CATEGORY_KEYS`, and `getCategoryLabel`
- `getCategoryLabel` returns the raw string for unknown keys (safe fallback)
</verification>

<success_criteria>
Both constant files exist with identical label mappings. Every subsequent plan in this phase can import from these files without duplicating the mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-slop-frontend-improvements/17-01-SUMMARY.md`
</output>
