---
phase: 10
slug: browse-first-ux
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-31
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for Phase 10: Browse-First UX.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (Tailwind utility classes only) |
| Icon library | none (emoji glyphs used in existing components) |
| Font | system-ui (Tailwind default, no custom font declared) |

Note: shadcn is not initialized. REQUIREMENTS.md DSG-01 through DSG-05 (visual design polish) are deferred to Phase 11. Phase 10 uses existing Tailwind utility patterns detected in the codebase — do not introduce new design tokens or visual changes.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding (`px-2 py-0.5`) |
| sm | 8px | Compact element spacing, button gaps |
| md | 16px | Default element spacing, card padding (`p-4`) |
| lg | 24px | Section padding, nav horizontal padding (`px-6`) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing (hero `py-20` = 80px nearest token) |

Exceptions:
- Mobile touch targets: minimum 44px height for all tappable elements (auth prompt buttons, bottom sheet action buttons)
- Bottom sheet handle area: 24px top padding before content

Source: Detected from existing `JobCard.jsx`, `NavBar.jsx`, `LandingPage.jsx` utility classes.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Label | 12px (`text-xs`) | 400 (regular) | 1.4 |
| Heading | 20px (`text-xl`) / `text-2xl` (24px) | 600 (semibold) | 1.2 |
| Display | 28px (`text-4xl` = 36px on hero) | 700 (bold) | 1.1 |

Notes:
- Body `text-sm text-gray-500` is the established pattern for secondary content (job description in `JobCard`).
- `font-semibold text-gray-900` is the established heading pattern within cards.
- No custom font is introduced in this phase. All sizes reference existing Tailwind scale.

Source: Detected from `JobCard.jsx` and `LandingPage.jsx`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#ffffff` (white) | Page backgrounds, card surfaces, nav background |
| Secondary (30%) | `#f9fafb` (gray-50) | Section backgrounds, hover states, unread notification rows |
| Accent (10%) | `#2563eb` (blue-600) | Primary CTA buttons, logo text, active links, category badge text, step indicators (client flow), auth modal primary button |
| Destructive | `#ef4444` (red-500) | Unread notification badge counter only; no destructive actions in this phase |

Accent reserved for: primary CTA buttons ("Log in to continue" / "Post a Job" / "Find Work"), logo wordmark, link hover color (`hover:text-blue-600`), category hover border/bg (`hover:border-blue-400 hover:bg-blue-50`), category badge (`bg-blue-100 text-blue-700`), step number circles (client).

Note: `green-600` is used for worker-flow step indicators in the existing landing page — preserve this; it is not a new token.

Source: Detected from `LandingPage.jsx`, `JobCard.jsx`, `NavBar.jsx`. Aligns with REQUIREMENTS.md DSG-01 ("neutral palette, grays, whites, one accent").

---

## Component Inventory

Components used or added in this phase:

| Component | Status | Notes |
|-----------|--------|-------|
| `JobCard.jsx` | Existing — reuse as-is | Dropped into landing page 6-job grid (D-07) |
| `NavBar.jsx` | Existing — modify | Must render for unauthenticated users; hide notification bell and sign-out; show Login/Register links instead |
| `ProtectedRoute.jsx` | Existing — extend | Add return URL threading: `?return={currentPath}` passed to `/login` redirect (D-05) |
| `AuthPromptModal` | New — web | Modal overlay for gated actions on web (D-03). Pattern: fixed overlay with centered card. |
| `AuthPromptBottomSheet` | New — mobile | Bottom sheet using `@gorhom/bottom-sheet` v5 for gated actions on mobile (D-03). |
| Landing 6-job section | New — inline in `LandingPage.jsx` | Grid of 6 `JobCard` instances with loading skeleton and empty state (D-07, D-08). |

---

## Interaction Contracts

### Auth Prompt Modal (Web)

Trigger: User clicks a gated action (Post a Job, Submit a Bid) while unauthenticated.

Behavior:
- Fixed overlay (`fixed inset-0 bg-black/50 z-50`) renders behind a centered card (`bg-white rounded-xl p-6 shadow-xl max-w-sm w-full mx-4`).
- Current URL is preserved — the page content remains visible beneath the overlay.
- Return URL is passed to login/register routes: `/login?return={currentPath}` and `/register?return={currentPath}`.
- Cancel button (secondary, text style) dismisses the modal without navigation.
- Focus trap inside modal while open.
- Closes on Escape key.

### Auth Prompt Bottom Sheet (Mobile)

Trigger: Same gated actions on mobile.

Behavior:
- Uses `@gorhom/bottom-sheet` (v5.2.8, already installed).
- `snapPoints`: `['35%']` — fixed half-sheet, not full screen.
- Same Login / Register / Cancel actions as web modal.
- Return destination threaded via navigation params.

### Landing Page 6-Job Grid

Behavior:
- Fetches `GET /api/jobs?status=open&limit=6&sort=createdAt:desc` on mount (anonymous, no auth header).
- Loading state: 6 skeleton cards matching `JobCard` dimensions (`h-24 bg-gray-100 rounded-xl animate-pulse`).
- Populated state: 6 `JobCard` instances in a `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`.
- "Browse all jobs →" link below the grid navigates to `/jobs` (D-08).
- Empty state: shown if API returns 0 jobs (see Copywriting Contract).

### NavBar — Unauthenticated State

- Logo link to `/` remains.
- Nav links shown: Jobs (`/jobs`), Browse Workers (`/workers`).
- Nav links hidden: My Jobs, Post a Job (these are gated; clicking triggers the auth modal from the destination page).
- Right side: "Log in" and "Register" text links instead of notification bell + profile + sign-out.

### Return URL Flow (Web)

1. Visitor on `/jobs/123` clicks "Submit a Bid".
2. Auth modal opens. Current path `/jobs/123` is captured.
3. Visitor clicks "Log in" → navigated to `/login?return=/jobs/123`.
4. After successful login, `LoginPage` reads `?return` param and calls `navigate(returnUrl)`.
5. Visitor lands back on `/jobs/123` — no auto-trigger of bid (D-06).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Auth modal heading | "Log in to continue" |
| Auth modal body | "Create a free account or log in to post a job or submit a bid." |
| Auth modal primary CTA | "Log in" |
| Auth modal secondary CTA | "Create account" |
| Auth modal dismiss | "Cancel" |
| Landing 6-job section heading | "Recent open jobs" |
| Landing 6-job empty state heading | "No open jobs yet" |
| Landing 6-job empty state body | "Be the first — post a job and get bids from skilled workers." |
| Landing 6-job empty state CTA | "Post a Job" |
| Landing 6-job loading state | (skeleton cards, no text) |
| Landing browse-all link | "Browse all jobs →" |
| Landing hero primary CTA | "Post a Job" (links to `/post-job`; triggers auth modal if unauthenticated) |
| Landing hero secondary CTA | "Find Work" (links to `/worker/browse`; now public) |
| Jobs page — unauthenticated, submit bid button | "Submit a Bid" (triggers auth modal) |
| Error state (API failure on landing grid) | "Couldn't load jobs. Try refreshing the page." |

Destructive actions: none in this phase.

Source: D-03, D-07, D-08 from CONTEXT.md. Copywriting fills gaps left by CONTEXT.md.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party component registries are used in this phase. `@gorhom/bottom-sheet` (already installed as a direct dependency in `mobile/package.json`) is not a shadcn registry block.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
